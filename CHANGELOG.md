# Changelog

## 2026-06-10（界面重构）

- 整体重做为移动端优先的卡片应用：底部导航（跟读 / 面试 / 思路），跟读改为全屏卡片组，左右滑动切题，下一张卡在边缘露出。
- 卡片内常驻显示回答（可滚动）；操作键纯图标化（只听答案 / 播放问→答 / 停止 / 循环）。
- 新增循环播放模式：播完当前卡自动滑到下一张接着播，到底循环回第一题。
- 滑动切卡时语音无缝接力到当前卡片；未播放时滑动保持安静。
- 题目总览底部抽屉（点顶栏「n/N」打开，绿点=已写回答），播放设置（语速/重复/间隔）收进抽屉。
- 视觉刷新：去描边浮卡 + 柔和投影、鲜亮蓝 #2f6bff 同色相渐变、顶部淡蓝光晕、分页圆点进度。

## 2026-06-10

- 调整标签页顺序：回答思路 → 共通質問 → 各学校（東大 生物統計情報 等依次排后）。

- 重组题库：`東大 生物統計情報` 只保留生物统计、研究计划、CCW / DML / 因果推断等专业相关问题。
- 合并通用问题：原 `共通質問`、原 `○○大学（例）`，以及从 `東大 生物統計情報` 中抽出的个人经历、日本留学/生活、兴趣、志望校/教授等问题，合并为一个 `共通質問` 标签。
- 优化跟读模式：主按钮改为一次点击按「问题 → 答案」顺序播放；占位答案不会被朗读。
- 增加音频缓存刷新：网页每次加载时给 mp3 请求加时间戳，避免浏览器继续播放旧音频。
- 扩展 `generate_audio.py`：支持 `--engine openai`、`--voice`、`--speed`、`--natural-pauses`、`--only`，并保留 edge-tts 备用方案。
- 全量重生成当前题库引用的 36 个音频文件：使用 OpenAI `gpt-4o-mini-tts`、`cedar` 声音、`speed=1.0`、自然停顿文本。
